import React from 'react';
import '../styles/Login.css';
import { LoginContainer } from '../containers/container/login/login.container';
export const Login = () => {
  return (
    <LoginContainer/>
  );
};
